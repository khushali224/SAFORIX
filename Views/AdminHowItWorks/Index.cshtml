@model IEnumerable<SAFORIX.Models.HowItWorksStep>
@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    ViewData["Title"] = "How It Works Steps";
}
<div class="admin-section">
    <div class="admin-section-header">
        <h2><i class="fas fa-list-ol"></i> How It Works Steps</h2>
        <a asp-action="Create" class="btn-add"><i class="fas fa-plus"></i> Add Step</a>
    </div>
    <div class="table-container">
        <table class="admin-table">
            <thead>
                <tr><th>#</th><th>Order</th><th>Step Title</th><th>Description</th><th>Active</th><th>Actions</th></tr>
            </thead>
            <tbody>
                @if (!Model.Any()) { <tr><td colspan="6" class="no-data">No steps found.</td></tr> }
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Id</td>
                        <td><strong>@item.StepOrder</strong></td>
                        <td>@item.StepTitle</td>
                        <td>@(item.Description?.Length > 70 ? item.Description.Substring(0, 70) + "…" : item.Description)</td>
                        <td>@(item.IsActive ? "✅" : "❌")</td>
                        <td>
                            <a asp-action="Edit" asp-route-id="@item.Id" class="btn-edit"><i class="fas fa-pen"></i></a>
                            <a asp-action="Delete" asp-route-id="@item.Id" class="btn-delete"><i class="fas fa-trash"></i></a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
